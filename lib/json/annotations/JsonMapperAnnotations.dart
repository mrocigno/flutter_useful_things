class JsonSerialize {
  const JsonSerialize._();
}

const JsonSerialize Serialize = JsonSerialize._();

class JsonField {
  final String name;
  const JsonField(this.name);
}

class JsonIgnore {
  const JsonIgnore();
}

const JsonIgnore Ignore = JsonIgnore();